#include <stdio.h>

int main() {
    int inputNum = 0;
    int sumDigits = 0;
    puts("Enter a number: ");
    scanf("%d",&inputNum);
    int totalDigits = inputNum;
    for (int position = 0; position < totalDigits; position += 2) {
,
}
